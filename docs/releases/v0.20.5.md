# Duo v0.20.5

## 首次启动

- 首次配置改为密码优先：环境、Codex / Claude、工作目录和端口由后台自动检测并填入默认值。
- 网络访问、SSH、工具路径和其他覆盖项收纳到“高级设置”，检测失败时自动展开手工配置。
- 自动检测尚未完成时不能提交，避免把未完成的默认值写入配置。
- 首屏窗口缩短，避免首次配置出现大片空白；工作台密码仍只保存哈希，不写入配置文件。

## 配置兼容

- 最小初始化配置会自动使用本机回环地址和安全默认环境；已有数据、密码、任务、凭据和环境配置不会被覆盖。
- 为首次配置增加回归检查，确认检测入口和高级设置边界不会被后续改动破坏。
