# Duo v0.20.12

## 新建任务自动检测执行环境

- 打开“新建任务”后会在后台自动检测本机 Windows 与 WSL 环境，不再要求先进入设置页手动检测。
- 环境选择器旁新增“重新检测”，检测过程中不会阻塞任务编辑；失败时可直接重试。
- 检测到的本机环境会合并保存到配置，保留已有环境 ID、SSH 配置、工作目录、模型和自定义工具路径；SSH 仍通过设置页手动配置。
- 设置页的环境检测保留为高级诊断入口。

升级会保留现有任务、知识库、工作模式和环境配置。
