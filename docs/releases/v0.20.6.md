# Duo v0.20.6

## 首次启动更简单

- 首次启动向导只要求设置工作台密码并确认密码。
- 执行环境、AI 工具、工作目录、网络访问、端口和 SSH 等机器相关配置不再阻塞首次进入。
- 初始化默认使用本机回环地址和安全的默认工作环境；进入工作台后可在“设置”中检测和配置 Windows、WSL、SSH、Codex、Claude Code、Harness 及远程访问。
- 未安装 AI 工具也可以先进入工作台，之后再补充工具路径和模型配置。

## 验证

- TypeScript 检查、Web 回归测试、Go 测试、C# 启动器合约测试和 Inno 安装器测试均通过。
